-- No-op: policies were already created successfully by migration 29
-- (which was re-applied with the corrected text[] return type for my_project_ids).
-- This migration exists only to keep the migration history consistent.
SELECT 1;
